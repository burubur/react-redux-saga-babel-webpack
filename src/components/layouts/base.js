import React, { Component } from 'react';

class Base extends Component {
    render() {
        return (
            <div>
                Base Layout
            </div>
        );
    }
}

export default Base;
